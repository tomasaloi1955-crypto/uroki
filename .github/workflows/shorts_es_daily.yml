name: Испанский канал Árabe Fácil — 3 шортса в день

on:
  schedule:
    - cron: "30 6 * * *"   # 09:30 МСК, через полчаса после английского
  workflow_dispatch:
    inputs:
      count:
        description: "Сколько роликов собрать и поставить в расписание"
        default: "3"

permissions:
  contents: write          # коммитим shorts_es_state.json обратно

concurrency:
  group: shorts-es-daily
  cancel-in-progress: false

jobs:
  post:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4
        with:
          ref: english-channel

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: ffmpeg и шрифты (арабская вязь)
        timeout-minutes: 12
        run: |
          for i in 1 2 3; do
            sudo apt-get update -q && \
            sudo apt-get install -y -q --no-install-recommends \
              ffmpeg fonts-noto-core fonts-dejavu-core && break
            echo "попытка $i не удалась, жду 20 сек..."; sleep 20
          done
          ffmpeg -version | head -1

      - name: python-библиотеки
        run: |
          pip install pillow arabic-reshaper python-bidi requests edge-tts \
            opencv-python-headless google-api-python-client google-auth-oauthlib

      - name: Собрать ролики и поставить в расписание
        env:
          YT_CLIENT_ID: ${{ secrets.YT_CLIENT_ID }}
          YT_CLIENT_SECRET: ${{ secrets.YT_CLIENT_SECRET }}
          YT_REFRESH_TOKEN: ${{ secrets.YT_REFRESH_TOKEN_ES }}
        run: python build_and_publish.py es ${{ github.event.inputs.count || 3 }}

      - name: Сохранить журнал публикаций
        run: |
          git config user.name "autopost-bot"
          git config user.email "bot@users.noreply.github.com"
          git add shorts_es_state.json
          git diff --cached --quiet || git commit -m "Árabe Fácil: очередь роликов пополнена [skip ci]"
          git pull --rebase origin english-channel
          git push
